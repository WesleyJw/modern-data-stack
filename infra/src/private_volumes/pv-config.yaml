apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-modern-stack-worker
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  hostPath:
    path: "/home/wesley/Minio/modern-data-stack/data"
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - modern-stack-worker

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-modern-stack-worker2
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  hostPath:
    path: "/home/wesley/Minio/modern-data-stack/data"
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - modern-stack-worker2